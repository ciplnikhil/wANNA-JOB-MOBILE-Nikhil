import {StyleSheet} from 'react-native';
import {Colors, Metrics, Fonts} from '../../Themes/';

export default StyleSheet.create({
    header: {
        width: '100%',
        height: 50,
        backgroundColor: '#71ba84',
      //   alignItems: 'center',
        justifyContent: 'center',
        paddingHorizontal: 20
    },
    headerText: {
        fontWeight: 'bold',
        color: '#fff',
        letterSpacing: 0.6
    },
});
